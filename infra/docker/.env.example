# Copy to .env in this directory for local development. Never commit the real .env file.

POSTGRES_USER=tst_autonomous
# Required, no built-in default. Generate with: openssl rand -base64 24
POSTGRES_PASSWORD=
POSTGRES_DB=tst_autonomous
POSTGRES_PORT=5432
